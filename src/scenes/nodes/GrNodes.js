import React, { Component } from 'react';
import {
    StyleSheet,
    View,
} from 'react-native';
import {
    InputGroup,
    Icon,
    Input,
    Text,
    Button,
    Spinner,
} from 'native-base';

export default class GroupNodes extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <View>
            </View>
        );
    }
}
